
                // y = y - depth;
                // width = width + 2 * depth;
                // height = height + 2 * depth;
                // g.drawArc(x, y, width, height, startAngle, arcAngle);